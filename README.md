# Six Degrees AI - Next.js Frontend

User-facing Next.js application for Six Degrees AI network search platform.

## Quick Start
```bash
npm install
npm run dev
```

Visit http://localhost:3000

## Environment Variables
Copy `.env.local.example` to `.env.local` and configure.

## Deployment
Automatically deploys to Vercel on push to main branch.

